#NOTE: This Configuration requires a ruby-enabled vim build
#Installation:
	
#    git clone git://github.com/samvit/dotvim.git ~/.vim
    mv ~/dotvim/ ~/.vim/
#Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

#Prerequisite: 
    sudo apt-get install ctags

#Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
